{
  "name": "draichiboard",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate-graphql-types": "npx graphql-codegen --config codegen.yml -r dotenv/config"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  }
}
